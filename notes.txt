
---
## xcpDerpNodes by lordwedggie

A ComfyUI custom nodes plugin — adds utility, UI, and workflow nodes.

**GitHub (intended)**: https://github.com/lordwedggie/xcp_derpNodes (private/not yet public — 404)
**Active workspace (2026-05-16)**: E:\Stable_Diffusion\ComfyUI-Easy-CU130_Developer\ComfyUI\custom_nodes\xcp_derp-UI\
**Prior path**: D:\xcpDerpNodes\ComfyUI-Easy-CU130_Developer\ComfyUI\custom_nodes\xcpDerpNodes\

### Structure
- __init__.py — entry point, ~15+ nodes (ANY, MX, Bool, Float, INT, STR, Samplers, Schedulers, Color Palette, Seed, Show Text, Theme Manager)
- python/ — derpStringV3, derpSliderAnyV4, derpThemeManagerV2, derpSeedV2, derpTemplate, derpPromptBook, xcpDerpLoraLoader
- js/, web/ — frontend
- xcp_file_server.py, Samplers.py
- Archives: _derpNodesArchieve/, _userArchieve/

Parent env: D:\xcpDerpNodes\ComfyUI-Easy-CU130_Developer\ (ComfyUI + many custom nodes)

---
Active workspace: E:\Stable_Diffusion\ComfyUI-Easy-CU130_Developer\ComfyUI\custom_nodes\xcp_derp-UI\ — main dev repo (branch: daily-development, remote: github). Key modules: derpControldeck, derpSeedV2, derpSignalOut, derpStringV3, derpTemplate, derpThemeManagerV2, xcpDerpLoraLoader. JS: controldeck/fatha/herbina/motha panels.

---
## xcpDerpNodes Working Rules (2026-05-15) — 4 Rules:
1. Think Before Coding: state assumptions, surface tradeoffs, ask when unclear.
2. Simplicity First: minimum code, no speculation, no unasked features/abstractions.
3. Surgical Changes: touch only what you must, match existing style, don't refactor adjacent code.
4. Goal-Driven Execution: define success criteria, loop until verified — weak criteria need constant clarification.
Active branch: daily-development | Workspace: E:\Stable_Diffusion\ComfyUI-Easy-CU130_Developer\ComfyUI\custom_nodes\xcp_derp-UI\

---
## xcpDerpNodes — Full Project Memory (2026-05-15)
Role: derp nodes frontend/backend integration. User=designer/PO, I=coder. Minimize regressions.
Core: protect unrelated local changes, minimal safe fixes, inspect before edit, fix one thing at a time, report only changed files.
Regression: inspect git commits/diffs first. UI bugs→panel/cache/rebuild. Output bugs→output assembly. Remove regression logic, don't patch symptoms.
LoRA/Trigger: bastaLoraDetail/derpLoraStack bugs→detail-panel/cache/rebuild first. Don't touch derpLoraStack output semantics without evidence. Don't modify stackData[4] for UI fixes. Trigger-save regression: preserve selection across transient empty-cache, prefer key identity over text matching.
Git: SSH-form commands, no commit/push/delete unasked.
Debug: English replies, targeted diagnostics only, don't remove TriggerWall/dock diagnostics.
Implementation: shared helpers over one-off patches, don't bloat large files, reuse existing geometry/layout helpers, ask before new abstractions if helper already large.
Fatha/Docking: vertical stacks must show titles. Horizontal stacks: resize width only. Vertical stacks: resize width only. Adding node to vertical stack must not change width. Pinned collapse: keep pinned stationary, reflow.
Palette/Theme: path=ComfyUI/user/derpNodes/Palettes/_system. Preserve lowercase compat. Theme JSON: _palette top-level. Absent→old header behavior. Per-node _palette resolution, not global. Palette=colors only, theme owns effects.
bastaPalette: FileBrowser/dropdown/preview/toggles synchronized. Deterministic cache/layout invalidation. Inspect COLORKEYEDIT/bastaColorDesigner/palette writeback before save changes.
Indicators: dropdown trigger=right-triangle, picker header=down-triangle. FileBrowser trigger=📁, picker first row=📂. Only explicit false/off/0 disables.
Behavior: ask when uncertain. Say if multiple files could be authority. State what you think issue is before risky edits.

---
## Active Project: xcpDerpNodes (2026-05-16)

ComfyUI custom nodes plugin by lordwedggie.

**Workspace:** E:\Stable_Diffusion\ComfyUI-Easy-CU130_Developer\ComfyUI\custom_nodes\xcp_derp-UI\ (branch: daily-development, remote: github)
**Prior path:** D:\xcpDerpNodes\ComfyUI-Easy-CU130_Developer\ComfyUI\custom_nodes\xcpDerpNodes\

**Full memory:** C:\Users\xuchuang\.deepseek\notes.txt — read this at session start for working rules, regression guidelines, layout conventions, palette/theme rules.

**4 Working Rules:** Think before coding, simplicity first, surgical changes, goal-driven execution.

---
## xcpDerpNodes — Latest State (2026-05-16)

### Release Repo
- Public repo: https://github.com/lordwedggie/xcp_derp-UI-Release
- Version: v1.0.4 (tagged, pushed)
- Clean history: single author lordwedggie
- themeManagerV2 + 4 helpers excluded
- ComfyUI Manager compatible

### Active Development
- Branch: daily-development
- Workspace: E:\Stable_Diffusion\ComfyUI-Easy-CU130_Developer\ComfyUI\custom_nodes\xcp_derp-UI

### Key Changes
- TriggerWall: serialization via properties.triggerGroups with weights/active; sys panel Deck profiles via showProfiles
- fathaSysPanel.js: triggerWallDeck added to isIndividual list
- Layout: group gaps, first group offset, remove button margin, bottom spacer
- derpPromptBook: page selector below editor
- derpLoraStack: app.graph.change() for serialization
- Dock Y-shift: rigid-body in moveDeck, reflow gated on allowCollapseShift
- Fatha: dockDebugHelpers shared, _cachedMapHash, _hashStamp, template literal cache key, unified drawNode hijack

---
## Session Memory Test (2026-05-16)

User is testing whether the desktop DeepSeek TUI can read notes.txt and remember between sessions. The file is at C:\Users\xuchuang\.deepseek\notes.txt.

This note was written using the `note` tool. If this persists across sessions, the note tool works.

---
## xcpDerpNodes — Hard-Learned Lessons

### 1. Diagnosis before coding
Map the pipeline end-to-end with logs BEFORE changing anything. One console.log at each stage pinpoints the blocker in one turn. Never guess.

### 2. Surgical edits — one file at a time
If a fix touches 3+ files, pause. Spraying changes creates self-inflicted bugs that cost more turns to debug than the original problem.

### 3. Don't break code while inserting diagnostics
Adding console.warn lines must preserve original logic exactly. In this case: replaced layout.compute() with a log, silently disabling removal-path force-height.

### 4. No ES module exports in ComfyUI custom node scripts
ComfyUI/Vite loads these as pooled scripts; `export const`/`export function` cause "Unexpected token 'export'" and "Identifier already declared". Use `window.fn = function(...){}` for shared utilities. Use `var` (not `const`) for module-level constants.

### 5. Verify after every reset
After git reset --hard, run `git diff` against the target branch to confirm ALL changes are present. Glance at each file's key sections — don't assume.

### 6. Fix at the source, not around it
derpLoraStack was not the problem. `resolveRuntimeDockSize` in dockDimensions.js was freezing height for horizontal docks via `Math.max(storedH, liveH, 0)`. Fix: `autoHeight ? engineFloorH : Math.max(storedH, liveH, 0)`. Framework fixes at the source are robust; workarounds at the symptom are fragile.

### 7. Avoid change amplification
When a simple fix doesn't work, resist adding MORE changes. Instead: revert all, verify baseline, add ONE change, test, repeat.

---
## Drag-and-Drop Ghost Height Pattern (derpSignalOut lesson)

**Problem:** Ghost row height must match original. `height: "auto"` recomputes from children; hardcoded values won't match.

**Pattern (3-step):**
1. Record original region height at dragStart into snapshot (e.g. `_signalOutFloatingSnapshot = { baseReg: { h: region.h } }`)
2. Ghost uses snapshot: `height: item.isPreviewGhost ? dragPlaceholderHeight : "auto"`
3. Clear snapshot on dragEnd/press

**Do NOT:** hardcode ghost height, use `height: "auto"` for ghost, or use `alpha:0` with frozen layout

**Symptoms of wrong height:** REGION border as 1-2px line, pulsing animations, children shrunk

**Debugging:** temporarily make ghost visible (alpha 0.5) to verify edits

---
"edit_file" and "apply_patch" frequent failure cause on Windows repos: write_file formats newlines as '\n' (Unix LF). Windows files expect '\r\n' (CRLF). After write_file, the file has mixed/unexpected line endings. Subsequent edit_file searches fail because it reads raw bytes (which now contain bare \n), while the search string may have been constructed with \r\n expectations. Prevention: never use write_file on Windows repos — always use edit_file or apply_patch instead.

---
# Public Release Publishing Process

## One-time setup
- Public repo remote: `public` → `lordwedggie/xcp_derpNodes-release`
- Private repo remote: `github` → `lordwedggie/xcp_derpNodes`

## Steps for each release

### 1. Create release branch from private daily-development
```
git checkout daily-development
git pull github daily-development
git checkout -b release/vX.Y.Z
```

### 2. Remove private-only files
```
git rm -r .deepseek python/derpThemeManagerV2.py js/motha/themeManagerV2.js js/motha/themeManagerV2_core.js
```
(also `.monkeycode` if present)

### 3. Fix __init__.py
- Remove `from .python.derpThemeManagerV2 import ...`
- Remove `NODE_CLASS_MAPPINGS.update(THEME_V2_NODES)`
- Remove `**THEME_V2_DISPLAY` from display mappings
- Keep `bundled_asset_sync` import and `sync_bundled_assets()` call

### 4. Bump versions
Update `package.json` and `pyproject.toml` to new version

### 5. Commit and push
```
git add -A
git commit -m "release X.Y.Z"
git push public release/vX.Y.Z
```

### 6. Update public main
Since private and public main have unrelated histories:
```
git checkout main
git reset --hard release/vX.Y.Z
git push public main --force
```

### 7. Restore workflow file if needed
If force-push overwrote `.github/workflows/publish_action.yml`:
```
git checkout <old-main-commit> -- .github/workflows/publish_action.yml
git commit --amend --no-edit
git push public main --force
```

### 8. Trigger publish
Run `publish_action.yml` workflow_dispatch from `main` branch on GitHub Actions.

### Exclusions (never in public)
- `.deepseek/`
- `.monkeycode/`
- `python/derpThemeManagerV2.py`
- `js/motha/themeManagerV2.js`
- `js/motha/themeManagerV2_core.js`
- Never mention derpThemeManagerV2 removal in release notes or README


---
## xcpDerpNodes Release Process

Remote: `release` → `git@github.com:lordwedggie/xcp_derp-UI-Release.git`

### Pre-release cleanup checklist

Before every release commit, verify:

1. **`.comfyignore` exists** at repo root and covers:
   - `_temp/` — dev files, other nodes' code (will get flagged as bundled malware)
   - `_bump.py`, `_fix_toml.py` — dev scripts
   - `derp_docs/` — Obsidian vault, large images
   - `notes.txt` — dev notes
   - `%SystemDrive/` — corrupted git tree entries
   - Do NOT include `user/` — bundled assets (Palettes, Themes, backgrounds) must ship

2. **`user/derpNodes/` is clean** — delete personal/test data:
   - `derpLoraStack/` — keep only `Default_Profile.json`, delete all test profiles
   - `derpTriggerWall/` — keep only `derpTriggerWall.json` + `triggers_template.json`
   - `derpPromptBook/` — keep empty (defaults created by bundled_asset_sync)
   - `nodeSettings/` — keep empty (personal autosave data)
   - Check for NSFW file names — they WILL get flagged

3. **No `%SystemDrive%/` garbage** in git — `git ls-files "%SystemDrive%"` should be empty

### Release steps

1. Bump version in `pyproject.toml` and `package.json` to X.Y.Z
2. **Read both files back** to verify the bump
3. Stage all changes: `git add -A`
4. Strip excluded files:
   ```
   git rm --cached js/motha/themeManagerV2.js js/motha/themeManagerV2_core.js python/derpThemeManagerV2.py
   git rm --cached -r .deepseek/
   git rm --cached notes.txt
   ```
5. Verify `.github/workflows/publish_action.yml` is still staged
6. Commit: `git commit -m "release vX.Y.Z"`
7. Sync main: `git push --force release daily-development:main`
8. Tag: `git tag -a vX.Y.Z -m "vX.Y.Z"` then `git push release vX.Y.Z`
9. Push release branch: `git push release daily-development:refs/heads/release/vX.Y.Z`
10. Restore excluded files: `git add js/motha/themeManagerV2.js js/motha/themeManagerV2_core.js python/derpThemeManagerV2.py .deepseek/ notes.txt` then `git commit -m "restore excluded files to daily-development"`
11. Push dev: `git push --force-with-lease github daily-development`

### Registry status after publish

Check: `https://api.comfy.org/nodes/xcp_derp-UI/versions`

| Status | Meaning |
|--------|---------|
| `NodeVersionStatusPending` | Normal — in review queue, wait |
| `NodeVersionStatusActive` | Published, visible in default channel |
| `NodeVersionStatusFlagged` | Something triggered the scanner — check `.comfyignore`, NSFW filenames, bundled code |

If a version is Flagged, you CANNOT republish the same version number. Bump and try again.

---
## xcpDerpNodes — NEVER publish themeManagerV2

These three files must be excluded from public releases:
- js/motha/themeManagerV2.js
- js/motha/themeManagerV2_core.js
- python/derpThemeManagerV2.py

Stripped via `git rm --cached` during the release commit (see Release Process above).

---
## Hard-Learned Lessons (2026-06-02)

### ICONBUTTON in ignoreLayout / floating rows
`height: "fill"` does NOT resolve in rows with `ignoreLayout: true`. Use explicit pixel height
from the source row: `height: sourceRowHeight` (where `sourceRowHeight = Number.isFinite(sourceRow?.h) ? sourceRow.h : "auto"`).
`width: "match"` then correctly sizes the button to match that explicit height.

### Theme paint overrides for structural regions
When a theme key (e.g. `header`) should override a node's structural paint, the fix goes in
`handleDrawCTX` (fathaHandler.js), NOT in the layout map. Resolve the theme paint with
`resolvePaintData(entity, "header", state)` and spread it into `bodyPaint`:
`{ ...bodyPaint, ...headerThemePaint, corners: ... }`. Also include the header paint
fingerprint in the canvas cache key so changes invalidate the cache.

### Tooltip highlight syntax
Use `{{t_toolTip_highlight::text}}` or `{{t_toolTip_warning::text}}` for highlighted tooltip
text. The highlight resolves to a palette color key `t_toolTip_highlight` or `t_toolTip_warning`.
NOT `{pm}` or any other made-up syntax.

### Commit message discipline
Always report the commit message to the user before pushing. They want to review what gets committed.


## CodeWhale Session Rules

### REPORT FILES CHANGED
After EVERY edit, report:
- Which file(s) were modified
- What changed (brief)

Do NOT wait for the user to ask. Report immediately after making changes.

### COMMIT MESSAGES
Always report the full commit message before pushing. The user wants to review it.


## Always-Visible Picker Items (2026-06-03)
Items with `alwaysVisible: true` bypass directory filtering in the FILEBROWSER picker.
Also support `hidePrefix: true` and `reservePrefix: true` for consistent row layout.
Use this instead of modifying picker rendering per-caller.
Example: palette "None" item uses `{ value: "", display: "None", alwaysVisible: true, hidePrefix: true, reservePrefix: true }`.
